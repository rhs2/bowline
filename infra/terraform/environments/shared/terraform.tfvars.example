# Copy to terraform.tfvars (git-ignored) or pass with -var-file.

aws_region        = "us-east-1"
github_repository = "rhs2/bowline"

# Optional
# create_oidc_provider  = false
# oidc_provider_arn     = "arn:aws:iam::000000000000:oidc-provider/token.actions.githubusercontent.com"
# deploy_role_name      = "bowline-github-deploy"
# state_bucket_name     = "bowline-terraform-state-000000000000"
# state_lock_table_name = "bowline-terraform-locks"
# pull_account_ids      = ["111111111111"]
# tags                  = { CostCenter = "platform" }
